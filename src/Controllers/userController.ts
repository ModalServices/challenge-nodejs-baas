import '@Utils/module-alias';
import { Request, Response, NextFunction } from 'express';

export class userController {
  
  listAllUsers = (Request: Request, Response: Response, NextFunction: NextFunction) => {

  };

  listUserDetails = (Request: Request, Response: Response, NextFunction: NextFunction) => {

  }

  saveUser = (Request:Request, Response: Response, NextFunction: NextFunction) => {

  }

} 
